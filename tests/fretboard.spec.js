const { addFretToString } = require("../utils/strings");

describe("the addFretToString function adds notes and returns a fretboard", () => {
  it("takes the note 5", () => {
    const result = addFretToString(5);
    const expectedResult = expect(result).toBe(expectedResult);
  });
});
